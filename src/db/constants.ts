/**
 * Dimensionality of the embedding vectors. Must match the chosen Ollama embedding
 * model. Decision (§7 in the project plan): bge-m3, 1024 dimensions – see
 * @/llm/embeddings.
 */
export const EMBEDDING_DIMENSIONS = 1024;
