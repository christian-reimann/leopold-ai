# Postgres (siehe docker-compose.yml)
DATABASE_URL=postgres://leopold:leopold@localhost:5432/leopold
POSTGRES_PASSWORD=leopold

# Redis (BullMQ)
REDIS_URL=redis://localhost:6379

# Ollama (lokale Embeddings)
OLLAMA_BASE_URL=http://localhost:11434

# LLM-Provider für Generierung (Structured Output, Bewerbungstexte)
ANTHROPIC_API_KEY=

# Deployment (docker-compose.yml + Caddy, siehe .github/workflows/deploy.yml)
IMAGE_TAG=latest
APP_DOMAIN=
APP_PORT=3000
HETZNER_API_TOKEN=
